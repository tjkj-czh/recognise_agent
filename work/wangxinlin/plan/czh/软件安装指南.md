# 用地识别智能体 — 软件安装与部署指南（企业级统一版）

| 项目 | 内容 |
|------|------|
| 文档版本 | v2.0 |
| 目标系统 | Ubuntu 22.04 LTS |
| CUDA版本 | 12.4 |
| Python版本 | 3.10.x（Conda环境） |
| 编写日期 | 2026-05-07 |

> 本指南统一并替换旧版本中的冲突配置，适用于企业落地场景。开发环境使用 Docker Compose；生产环境建议使用 K8s。

---

## 一、安装前检查（必须）

### 1.1 硬件建议

- CPU：16 核以上
- 内存：64GB 以上
- GPU：24GB 显存以上（Qwen2-VL-7B 推荐）
- 磁盘：1TB NVMe（模型与影像数据）

### 1.2 软件前置

- 已安装 NVIDIA 驱动（与 CUDA 12.4 兼容）
- 可访问内部制品仓库或官方源
- 具备 sudo 权限

### 1.3 统一目录规范

```bash
mkdir -p ~/tjkj/{docker,logs,models,data,backups}
cd ~/tjkj
```

---

## 二、系统基础依赖

```bash
sudo apt update && sudo apt upgrade -y
sudo apt install -y \
  git curl wget vim jq unzip \
  build-essential pkg-config \
  libgl1-mesa-glx libglib2.0-0 libsm6 libxext6 libxrender-dev \
  gdal-bin libgdal-dev libproj-dev proj-bin libspatialindex-dev

gdal-config --version
```

---

## 三、安装 Docker 与 Compose

```bash
sudo apt remove -y docker docker-engine docker.io containerd runc 2>/dev/null || true
sudo apt install -y ca-certificates gnupg lsb-release
sudo mkdir -p /etc/apt/keyrings
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | \
  sudo gpg --dearmor -o /etc/apt/keyrings/docker.gpg

echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu \
$(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

sudo apt update
sudo apt install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin
sudo usermod -aG docker $USER
newgrp docker

docker --version
docker compose version
```

---

## 四、安装 NVIDIA Container Toolkit（GPU容器支持）

```bash
curl -fsSL https://nvidia.github.io/libnvidia-container/gpgkey | \
  sudo gpg --dearmor -o /usr/share/keyrings/nvidia-container-toolkit-keyring.gpg

curl -s -L https://nvidia.github.io/libnvidia-container/stable/deb/nvidia-container-toolkit.list | \
  sed 's#deb https://#deb [signed-by=/usr/share/keyrings/nvidia-container-toolkit-keyring.gpg] https://#g' | \
  sudo tee /etc/apt/sources.list.d/nvidia-container-toolkit.list

sudo apt update
sudo apt install -y nvidia-container-toolkit
sudo nvidia-ctk runtime configure --runtime=docker
sudo systemctl restart docker

docker run --rm --gpus all nvidia/cuda:12.4.0-base-ubuntu22.04 nvidia-smi
```

---

## 五、安装 Miniconda 与 Python 3.10

```bash
wget -O ~/Miniconda3-latest-Linux-x86_64.sh https://repo.anaconda.com/miniconda/Miniconda3-latest-Linux-x86_64.sh
bash ~/Miniconda3-latest-Linux-x86_64.sh -b -p $HOME/miniconda3

echo 'export PATH="$HOME/miniconda3/bin:$PATH"' >> ~/.bashrc
source ~/.bashrc
conda init bash
source ~/.bashrc

conda create -n landuse python=3.10 -y
conda activate landuse
python --version
```

---

## 六、安装后端依赖（统一推荐）

### 6.1 PyTorch（CUDA 12.4）

```bash
conda activate landuse
pip install --upgrade pip
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu124
python -c "import torch; print(torch.__version__, torch.cuda.is_available())"
```

### 6.2 服务与任务依赖

```bash
pip install \
  fastapi==0.115.0 uvicorn[standard]==0.30.6 pydantic==2.9.0 \
  sqlalchemy==2.0.35 asyncpg==0.29.0 psycopg2-binary==2.9.9 alembic==1.13.2 \
  celery==5.4.0 redis==5.1.1 flower==2.0.1 \
  python-multipart==0.0.9 python-jose==3.3.0 \
  httpx==0.27.2 loguru==0.7.2 websockets==13.1 pandas==2.2.3 minio==7.2.9
```

### 6.3 遥感依赖（企业可执行兜底）

```bash
# 优先 pip，失败时切 conda-forge（推荐兜底）
pip install rasterio==1.3.10 geopandas==1.0.1 shapely==2.0.6 pyproj==3.6.1 fiona==1.9.6 rio-cogeo==5.3.1 || \
conda install -y -c conda-forge rasterio geopandas shapely pyproj fiona
```

### 6.4 VLM 推理依赖

```bash
pip install transformers==4.44.0 huggingface-hub==0.24.6 accelerate==0.33.0 vllm==0.6.0
```

---

## 七、前端依赖

```bash
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs
node -v
npm -v
npm install -g pnpm
pnpm -v
```

---

## 八、模型权重准备（修正后）

```bash
mkdir -p ~/tjkj/models
conda activate landuse

# 1) SAM权重（ViT-H）
wget -P ~/tjkj/models https://dl.fbaipublicfiles.com/segment_anything/sam_vit_h_4b8939.pth

# 2) DeepLabV3+（建议通过torchvision自动加载或内部模型仓统一分发）
python -c "import torchvision; m=torchvision.models.segmentation.deeplabv3_resnet101(weights='DEFAULT'); print('DeepLab ready')"

# 3) Qwen2-VL-7B-Instruct（VLM）
python -c "from huggingface_hub import snapshot_download; snapshot_download(repo_id='Qwen/Qwen2-VL-7B-Instruct', local_dir='$HOME/tjkj/models/Qwen2-VL-7B-Instruct', local_dir_use_symlinks=False)"
```

> 说明：旧版文档中“DeepLab下载命令误下载SAM权重”的问题已修正。

---

## 九、配置管理（安全版）

### 9.1 创建 `.env`（示例）

```bash
cat > ~/tjkj/.env << 'EOF'
APP_ENV=dev
APP_NAME=landuse-agent

DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=landuse_agent
DB_USER=landuse
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD
DB_URL=postgresql+asyncpg://landuse:CHANGE_ME_STRONG_PASSWORD@127.0.0.1:5432/landuse_agent

REDIS_URL=redis://127.0.0.1:6379/0

MINIO_ENDPOINT=127.0.0.1:9000
MINIO_ACCESS_KEY=landuse
MINIO_SECRET_KEY=CHANGE_ME_STRONG_PASSWORD
MINIO_SECURE=false
MINIO_BUCKET_IMAGES=images
MINIO_BUCKET_TILES=tiles
MINIO_BUCKET_PATCHES=patches
MINIO_BUCKET_EXPORTS=exports

VLM_API_URL=http://127.0.0.1:8001/v1
VLM_MODEL_NAME=Qwen/Qwen2-VL-7B-Instruct

MODEL_DIR=/home/$USER/tjkj/models
SAM_WEIGHTS=/home/$USER/tjkj/models/sam_vit_h_4b8939.pth

CUDA_VISIBLE_DEVICES=0
EOF
```

### 9.2 安全要求

- `.env` 不入库（加入 `.gitignore`）
- 生产环境禁止使用示例密码
- 生产环境建议用密钥管理服务替代 `.env`

---

## 十、基础中间件部署（Docker Compose）

在 `~/tjkj/docker/docker-compose.yml` 写入：

```yaml
version: "3.8"
services:
  postgres:
    image: postgis/postgis:16-3.4
    container_name: landuse-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: landuse_agent
      POSTGRES_USER: landuse
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U landuse -d landuse_agent"]
      interval: 10s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: landuse-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    command: redis-server --maxmemory 1gb --maxmemory-policy allkeys-lru
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5

  minio:
    image: minio/minio:latest
    container_name: landuse-minio
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: ${MINIO_ACCESS_KEY}
      MINIO_ROOT_PASSWORD: ${MINIO_SECRET_KEY}
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - miniodata:/data
    command: server /data --console-address ":9001"

volumes:
  pgdata:
  miniodata:
```

启动：

```bash
cd ~/tjkj/docker
set -a; source ~/tjkj/.env; set +a
docker compose up -d
docker compose ps
```

---

## 十一、VLM服务启动

```bash
conda activate landuse
mkdir -p ~/tjkj/logs
nohup python -m vllm.entrypoints.openai.api_server \
  --model ~/tjkj/models/Qwen2-VL-7B-Instruct \
  --served-model-name Qwen/Qwen2-VL-7B-Instruct \
  --host 0.0.0.0 --port 8001 \
  --gpu-memory-utilization 0.85 \
  --max-model-len 4096 \
  --trust-remote-code \
  > ~/tjkj/logs/vlm_server.log 2>&1 &

curl http://127.0.0.1:8001/v1/models
```

---

## 十二、健康检查与验收

```bash
# PostgreSQL
python -c "import asyncio; from sqlalchemy.ext.asyncio import create_async_engine; \
async def t():\n e=create_async_engine('postgresql+asyncpg://landuse:CHANGE_ME_STRONG_PASSWORD@127.0.0.1:5432/landuse_agent');\n async with e.connect() as c: r=await c.execute(__import__('sqlalchemy').text('SELECT PostGIS_Version()')); print(r.scalar());\n await e.dispose();\nasyncio.run(t())"

# Redis
python -c "import redis; r=redis.Redis(host='127.0.0.1',port=6379); r.set('health','ok'); print(r.get('health').decode())"

# MinIO
python -c "from minio import Minio; c=Minio('127.0.0.1:9000', access_key='landuse', secret_key='CHANGE_ME_STRONG_PASSWORD', secure=False); print('minio ok')"

# VLM
python -c "import httpx; print(httpx.get('http://127.0.0.1:8001/v1/models').status_code)"
```

---

## 十三、企业运维建议（生产）

- 使用 K8s 部署应用服务与Worker，配置 HPA
- PostgreSQL 与 MinIO 做定时备份与恢复演练
- 接入 Prometheus + 告警策略
- 设置发布回滚策略（蓝绿/金丝雀）
- 建立变更审批与值班响应机制

---

## 十四、常见问题

### Q1：rasterio/GDAL 编译失败

优先使用 `conda-forge` 安装栈，避免系统依赖不匹配。

### Q2：VLM显存不足

降低 `--gpu-memory-utilization`，必要时启用量化模型或仅对争议图斑裁判。

### Q3：容器服务正常但外部不可访问

检查安全组/防火墙及监听地址；生产建议仅通过网关暴露。

### Q4：凭据泄露风险

立即轮换密码，清理日志与历史，启用密钥管理并禁止明文提交。
